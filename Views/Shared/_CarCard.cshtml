@model AmiFlota.Models.CarModel
@{
    var photoPath = "/images/cars/" + (Model.PhotoPath ?? "default") + ".jpg";

}
<div class="col">
    <div class="card h-100">
        <img src="@photoPath" class="card-img-top img-fluid" alt="Unable to load car photo">
        <div class="card-body">
            <h4 class="card-title text-center text-uppercase">@Model.Brand @Model.Model</h4>
            <h5 class="card-title text-center text-uppercase">@Model.RegistrationNumber</h5>
            <p class="card-text">This is a longer card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
        </div>
        <div class="card-footer">

            @*WITHOUT Ajax*@
            <a class="btn btn-primary" asp-action="Details" asp-route-id="@Model.VIN">Show details</a>

            @*
            //TODO Not working
            <button class="btn btn-outline-secondary btn-details" id="btnDetails" data-vin="@Model.VIN" type="button">Details</button>
            *@
        </div>
    </div>
</div>

@*
   //TODO Not working
<script>
    $(".btn-details").on('click', function() {

        var VIN = $(this).data("vin");
        console.log(VIN);

        $.ajax({
            url: '@Url.Action("Details", "Car")',
            datatype: 'html',
            data: { id: VIN },
            method: 'GET',

            success: function(result) {
            },

            error: function(error) {
                console.log(error);
            }
        });
    });
</script>*@

