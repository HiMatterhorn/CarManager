@model AmiFlota.Models.ViewModels.BookingVM

<!-- Button trigger modal -->
<!-- Modal -->
<div class="modal fade" id="bookingDetailsModal" tabindex="-1" role="dialog" aria-labelledby="bookingDetailsModal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <form method="post" asp-controller="Booking" asp-action="_BookingDetailsModal" id="bookingDetailsModalForm">
                <div class="modal-header">
                    <h5 class="modal-title" id="bookingDetailsModalTitle">Booking details</h5>
                </div>

                @*//TODO Layout corrections*@
                <div class="modal-body">
                    @*  <div asp-validation-summary="ModelOnly" class="text-danger">*@
                    <div class="form-group row">
                        <div class="col-6 text-right">
                            <label for="lblUserName">User Name: </label>
                        </div>

                        <div class="col-6 text-left">
                            <label for="lblUserNameValue">@Model.UserName</label>
                            <input asp-for="UserName" type="hidden"></>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="RegistrationNumber">Registration Number: @Model.RegistrationNumber</label>
                        <input asp-for="RegistrationNumber" type="hidden"></>
                    </div>

                    <div class="form-group">
                        <label for="StartDate">Start date: @Model.StartDate</label>
                        <input asp-for="StartDate" type="hidden"></>
                    </div>

                    <div class="form-group">
                        <label for="EndDate">End date: @Model.EndDate</label>
                        <input asp-for="EndDate" type="hidden"></>
                    </div>


                    <div class="form-group">
                        <label asp-for="Destination">Destination</label>
                        <input asp-for="Destination" class="form-control" id="Booking.Destination"></>
                        <span asp-validation-for="Destination"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="ProjectCost">Project:</label>
                        <input asp-for="ProjectCost" class="form-control" id="Booking.ProjectCost"></>
                        <span asp-validation-for="ProjectCost"></span>
                    </div>
                    @*    </div>*@
                </div>

                <div class="modal-footer">
                    <div class="row align-items-center">
                        <button type="submit" id="btnBookCar" class="btn btn-success btn-book">Book</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>



<script>

    @*   Ajax call option
        $("#btnBook").click(function() {
        var Destination = document.getElementById("inDestination").value;
        var ProjectCost = document.getElementById("inProjectCost").value;
        });


        //TODO Rethink
        @*$.ajax({
        // url: '@Url.Action("BookingDetails", "Booking")',
        routeURL + '/api/Booking/GetCalendarDataById/'
        datatype: 'JSON',
        data: { vin : VIN, startDate: StartDate, endDate: EndDate },
        method: 'GET',

        success: function(result) {
        console.log("OK");
        },

        error: function(error) {
        console.log(error);
        }
        });*@
</script>